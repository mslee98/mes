# ItemForm.tsx 한글 복원 참고

에디터/저장 시 한글이 `??`로 깨질 때 아래 문자열로 치환하면 됩니다.

## 주석
- `/** ?? ? ??? ??? (isNew/item ???? ?? ??? ??) */` → `/** 수정 시 비교용 초기값 (isNew/item 기준으로 렌더 시점에 계산) */`
- `// ???? item ?? ? ? state ???` → `// 서버에서 item 로드 시 폼 state 동기화`

## 옵션 라벨
- 분류: `label: "?? ??"` (categoryOptions) → `label: "분류 선택"`
- 유형: `label: "?? ??"` (itemTypeOptions) → `label: "유형 선택"`
- 단위: `label: "?? ??"` (unitOptions) → `label: "단위 선택"`
- 기존값: `(???)` → `(기존값)`
- 통화: `label: "? (KRW)", symbol: "?"` → `label: "원 (KRW)", symbol: "원"`

## 토스트
- 등록 성공: `toast.success("??? ???????.")` → `toast.success("품목이 등록되었습니다.")`
- 수정 성공: `toast.success("??? ???????.")` → `toast.success("품목이 수정되었습니다.")`
- 등록 실패: `"??? ??????."` (createMutation onError) → `"등록에 실패했습니다."`
- 수정 실패: `"??? ??????."` (updateMutation onError) → `"수정에 실패했습니다."`
- 품목 코드: `toast.error("?? ??? ?????.")` → `toast.error("품목 코드를 입력하세요.")`
- 품목명: `toast.error("???? ?????.")` → `toast.error("품목명을 입력하세요.")`
- 품목 분류: `toast.error("?? ??? ?????.")` → `toast.error("품목 분류를 선택하세요.")`
- 품목 유형: `toast.error("?? ??? ?????.")` → `toast.error("품목 유형을 선택하세요.")`

## 페이지 메타 / 로딩
- 로딩 시: `title="?? ??" description="?? ??"` → `title="품목 수정" description="품목 수정"`
- 로딩 메시지: `message="?? ??? ???? ?..."` → `message="품목 정보를 불러오는 중..."`
- 등록/수정: `"?? ??"` → `"품목 등록"` / `"품목 수정"` (상황에 맞게)
- 카드 설명: `desc="?? ??? ??? ??? ? ??, ???, ?? ?? ?????."` → `desc="품목 분류와 유형을 선택한 뒤 코드, 품목명, 단가 등을 입력하세요."`

## 폼 라벨 / 플레이스홀더
- 품목 코드 *, placeholder "?? ??" → "품목 코드 *", "품목 코드"
- 품목명 *, placeholder "???" → "품목명 *", "품목명"
- 품목 분류 *, 품목 유형 *, 단위, 단가, 규격, 제품 구분, 활성
- selectPlaceholder "?? ??" → "통화 선택"
- placeholder "??" (규격) → "규격"
- placeholder "?? ??" (제품 구분) → "제품 구분"

## 버튼
- `{isPending ? "?? ?..." : isNew ? "??" : "??"}` → `{isPending ? "저장 중..." : isNew ? "등록" : "수정"}`
- 취소 버튼 텍스트 `??` → `취소`
